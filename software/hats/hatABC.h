#ifndef HAT_ABC_H
#define HAT_ABC_H

#include "Arduino.h"

class HatABC {
private:

    // the 6 bit sequence for this unique hat - last 2 bits are always 0
    const static uint8_t hatID;

public:

};

#endif
